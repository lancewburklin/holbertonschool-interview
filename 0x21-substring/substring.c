#include <stdlib.h>
#include <stdio.h>

#include "substring.h"

/**
 * find_substring - Find a substring
 * @s: String to search
 * @words: Words to use
 * @nb_words: Number of words to hnt for
 * @n: Where to store all numbers found
 * Return: Location of substrings
*/

int *find_substring(char const *s, char const **words, int nb_words, int *n)
{
	if (s == NULL)
		return (NULL);
	if (words == NULL)
		return (NULL);
	if (nb_words == 0)
		return (NULL);
	return (n);
}
